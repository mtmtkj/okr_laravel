linter:
  code_sniffer:
    options:
      standard: PSR2
      dir: app/
  phpmd:
    target:
      - app
    options:
      rule: cleancode,codesize,controversial,design,naming,unusedcode,phpmd.xml
